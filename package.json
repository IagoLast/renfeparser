{
  "name": "renfeparser",
  "version": "1.0.0",
  "description": "REST API for RENFE train schedules.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@iagolast",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^0.22.0",
    "cors": "^2.8.1",
    "eslint": "^3.18.0",
    "express": "^4.14.0",
    "request": "^2.75.0"
  },
  "engines": {
    "node": "4.3.1"
  }
}
